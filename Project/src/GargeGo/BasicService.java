package GargeGo;

public class BasicService extends Service {
    public BasicService(String serviceId, String name, String description, String priceRange) {
        super(serviceId, name, description, priceRange);
    }
}
